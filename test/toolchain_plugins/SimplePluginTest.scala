package test.toolchain_plugins

// Simple test that doesn't require any special plugin features
// This verifies the basic compilation works
object SimplePluginTest {
  def test(): String = {
    "Global plugin mechanism works"
  }
}