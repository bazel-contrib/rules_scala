# rules_jvm_external's coursier_fetch defaults resolve_timeout to 600s for
# the whole maven.install() -- too tight for joern's ~300 artifacts (doubled
# by fetch_sources) under this repo's CI network; build #6380 timed out at
# ~600s resolving them. Bump well past that near-miss.
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -93,7 +93,8 @@
         "org.scala-lang:scala3-library_3",
         "org.scala-lang:scala-library",
     ],
     fetch_sources = True,
+    resolve_timeout = 1800,
     lock_file = "//:maven_install.json",
     repositories = [
         "https://repo1.maven.org/maven2",
