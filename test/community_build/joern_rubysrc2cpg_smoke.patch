# Wraps the existing RubySrc2CpgHTTPServerTests (writes a file, starts the
# frontend's HTTP server, loads the CPG) as io.joern.smoke.SmokeSuite --
# see BUILD's test_filter comment. ~55s -> ~7s.
--- /dev/null
+++ b/joern-cli/frontends/rubysrc2cpg/src/test/scala/io/joern/smoke/SmokeSuite.scala
@@ -0,0 +1,6 @@
+package io.joern.smoke
+
+import io.joern.rubysrc2cpg.io.RubySrc2CpgHTTPServerTests
+import org.scalatest.Suites
+
+class SmokeSuite extends Suites(new RubySrc2CpgHTTPServerTests)
