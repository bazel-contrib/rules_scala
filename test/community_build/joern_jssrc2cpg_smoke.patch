# Wraps 6 existing classes (AST, CFG, dataflow, type-recovery passes, plus
# a file parse and an HTTP round trip) as io.joern.smoke.SmokeSuite -- see
# BUILD's test_filter comment for why the package is io.joern.smoke, not
# io.joern.jssrc2cpg. ~800s -> ~165s.
--- /dev/null
+++ b/joern-cli/frontends/jssrc2cpg/src/test/scala/io/joern/smoke/SmokeSuite.scala
@@ -0,0 +1,18 @@
+package io.joern.smoke
+
+import io.joern.jssrc2cpg.dataflow.DataflowTests
+import io.joern.jssrc2cpg.io.{JsSrc2CpgHTTPServerTests, ProjectParseTests}
+import io.joern.jssrc2cpg.passes.TypeRecoveryPassTests
+import io.joern.jssrc2cpg.passes.ast.SimpleAstCreationPassTests
+import io.joern.jssrc2cpg.passes.cfg.SimpleCfgCreationPassTests
+import org.scalatest.Suites
+
+class SmokeSuite
+    extends Suites(
+      new SimpleAstCreationPassTests,
+      new SimpleCfgCreationPassTests,
+      new DataflowTests,
+      new TypeRecoveryPassTests,
+      new ProjectParseTests,
+      new JsSrc2CpgHTTPServerTests
+    )
